{
  "name": "wave-shell",
  "main": "dist/index.js",
  "type": "module",
  "version": "0.0.20",
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "bun-types": "latest"
  },
  "bin": {
    "wave-shell": "bin/wave"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun run src/main.ts",
    "build": "bun src/builder.ts",
    "publish": "bun run build && bun test && npm publish"
  },
  "dependencies": {
    "cli-table3": "^0.6.3",
    "bun-plugin-dts": "^0.2.1",
    "require-all": "^3.0.0",
    "surfstar": "^0.0.2",
    "zod": "^3.22.4"
  }
}
